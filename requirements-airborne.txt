# Install a JetPack-compatible PyTorch build separately before using Ultralytics.
Flask>=3.0,<4
Flask-SocketIO>=5.3,<6
numpy>=1.24,<2
opencv-python>=4.8,<5
pymavlink>=2.4,<3
scipy>=1.10,<2
ultralytics>=8.2,<9

# Optional for Intel RealSense D455 input. Jetson users may need Intel's native
# librealsense package instead of the PyPI wheel.
pyrealsense2>=2.54; platform_machine != "aarch64"
